{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@manlydude/web",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/ai/**/*.ts": {
      "maxDuration": 30
    }
  }
}
